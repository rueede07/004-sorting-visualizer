# Sorting Visualizer

Small Python project to better understand sorting algorithms.

Currently implemented:
- bubble sort
- selection sort

The program prints every swap and list update step-by-step.

Run with:

```bash
python3 main.py